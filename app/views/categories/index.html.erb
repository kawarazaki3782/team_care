<div>
    <% @categories.each do |category| %>
        <%= category.name %>
    
     <%= link_to '削除', category, method: :delete,
                                 data: { confirm: "You sure?" }  %>
    <% end %>
</div>